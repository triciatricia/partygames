/**
 * Actually a string. Only complains when warning on VERBOSE
 *
 * You have to run in VERBOSE warning mode in order for Closure to complain:
 *
 * ../closure-compiler.jar --warning_level VERBOSE --js bad_type.js
 */

/**
 * @type {number}
 */
var int_val = "test";
